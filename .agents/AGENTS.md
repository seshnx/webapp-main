# Agent Rules & Guidelines

- Never use mock, synthetic, hardcoded, or dummy data arrays. If a data source or API is missing nor empty, ask the user for DB build outs and back end build instead of generating fake values.
