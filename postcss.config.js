export default {
  plugins: {
    // This plugin runs Tailwind CSS
    tailwindcss: {},
    // This plugin adds necessary vendor prefixes (like -webkit-) for compatibility
    autoprefixer: {},
  },
}
