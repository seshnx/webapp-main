# ============================================
# VERCEL PRODUCTION ENVIRONMENT VARIABLES
# ============================================
# This file shows which environment variables need to be set in Vercel Dashboard.
# Go to: Project Settings > Environment Variables
#
# IMPORTANT: Set these in Vercel Dashboard, NOT in .env files!

# ============================================
# FRONTEND VARIABLES (Accessible to Browser)
# ============================================
# These MUST be prefixed with VITE_ for Vite to access them in client-side code

# Neon PostgreSQL Database
VITE_NEON_DATABASE_URL=postgresql://neondb_owner:password@ep-xxx.pooler.region.aws.neon.tech/neondb?sslmode=require

# Vercel Blob Storage (automatically set by Vercel, but you can add VITE_ version)
VITE_BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx

# Clerk Authentication (Public Key)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxx

# Convex Real-time
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Stripe Payments (Public Key)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx

# API Endpoints
VITE_API_ENDPOINT=https://api.seshnx.com

# Supabase (Legacy - Being Migrated)
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...

# Sentry Error Tracking (Public DSN)
VITE_SENTRY_DSN=https://xxx@sentry.io/xxx

# ============================================
# BACKEND VARIABLES (Server-Side Only)
# ============================================
# These are ONLY accessible in API routes, NOT exposed to browser

# Clerk Authentication (Secret Key)
CLERK_SECRET_KEY=sk_test_xxxxxxxx

# Stripe Payments (Secret Key)
STRIPE_SECRET_KEY=sk_test_xxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx

# Sentry (Server-side Auth Token)
SENTRY_AUTH_TOKEN=your-auth-token-here

# Convex Deployment Key
CONVEX_DEPLOY_KEY=your-deploy-key-here

# ============================================
# AUTOMATICALLY SET BY VERCEL
# ============================================
# These are automatically available in your Vercel deployment:
# - VERCEL_URL
# - VERCEL_ENV (production, preview, development)
# - VERCEL_GIT_COMMIT_SHA
# - VERCEL_GIT_REPO_SLUG

# ============================================
# OPTIONAL VARIABLES
# ============================================
# Only add these if you're using these features:

# Google OAuth (for Clerk)
GOOGLE_OAUTH_CLIENT_ID=your-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Other OAuth Providers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Email Service (if sending transactional emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Go to Vercel Dashboard: https://vercel.com/dashboard
# 2. Select your project: seshnx-webapp
# 3. Go to Settings > Environment Variables
# 4. Add each variable from the list above
# 5. Select the correct environments (Production, Preview, Development)
# 6. Redeploy your application after adding variables
#
# Note: For Production, use production credentials (not test keys)
